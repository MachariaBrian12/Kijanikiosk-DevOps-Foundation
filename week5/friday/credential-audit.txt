# KijaniKiosk CI Pipeline — Credential Audit
# Run against final committed Jenkinsfile and git history
# Date: 2026-04-19

=============================================================
CHECK 1: Credentials in Jenkinsfile
=============================================================
Command: grep -iE 'password|secret|token|_auth|NEXUS_PASS|NEXUS_USER' Jenkinsfile

Result:
  usernameVariable: 'NEXUS_USER',
  passwordVariable: 'NEXUS_PASS'

Assessment: PASS
These are variable name declarations inside a withCredentials block.
They reference Jenkins credential IDs, not literal credential values.
No passwords, tokens, or secrets appear as literal strings.

=============================================================
CHECK 2: Literal credentials in Jenkinsfile
=============================================================
Command: grep -iE '[a-zA-Z0-9+/]{20,}|password\s*=\s*["\x27][^"\x27]+' Jenkinsfile

Result: No matches found.

Assessment: PASS
No base64-encoded strings or inline password assignments found.

=============================================================
CHECK 3: Credentials in git commit history
=============================================================
Command: git log -p | grep -iE 'password|secret|token|_auth' | grep -v 'Variable|credentialsId|passwordVariable'

Result: No matches found.

Assessment: PASS
No credential values appear in any commit in the repository history.

=============================================================
CHECK 4: Credentials in build log
=============================================================
Command: grep -iE 'password|secret|_auth' green-pipeline-run.txt

Result:
  [Pipeline] withCredentials
  Masking supported pattern matches of $NEXUS_USER or $NEXUS_PASS
  echo "****:_auth=****"

Assessment: PASS
Jenkins credential masking is active. All credential values are
replaced with **** in the build log. No literal values exposed.

=============================================================
CHECK 5: .npmrc file not committed to repository
=============================================================
Command: git ls-files | grep npmrc

Result: No matches found.

Assessment: PASS
The .npmrc file is created and deleted within the same shell step
in the Publish stage. It is never written to the workspace
between pipeline steps and is not tracked by git.

=============================================================
SUMMARY
=============================================================
Check 1 — Credentials in Jenkinsfile:     PASS
Check 2 — Literal credentials in code:    PASS
Check 3 — Credentials in git history:     PASS
Check 4 — Credentials in build log:       PASS
Check 5 — .npmrc not committed:           PASS

All five checks passed. No credentials leaked into the
Jenkinsfile, git history, or build logs.
